# AgentLearn 优化任务清单（持续更新）

> 说明：本清单基于最初优化计划（P0/P1/P2）整理。后续每完成一项，请把对应复选框从 `- [ ]` 改为 `- [x]`。

更新时间：2026-03-16（P0/P1 已完成）

---

## P0（必须优先）

### 1) 分层重构（入口/编排/领域/基础设施）
- [x] 建立 `config/`、`core/`、`infra/` 基础结构
- [x] 抽离 LLM 客户端到 `infra/llm/openai_compatible.py`
- [x] 抽离策略逻辑到 `core/domain/`
- [x] 拆分 `workflow/state/nodes` 目录并将主流程迁移
- [x] 继续拆分 `rag_engine.py`（降低单文件复杂度）

### 2) 可观测闭环
- [x] 每轮请求 `trace_id`
- [x] 结构化日志事件（request/retrieval/response）
- [x] 基础运行指标（请求数、检索命中/未命中）
- [x] 日志敏感字段脱敏
- [x] 接入观测平台（OpenTelemetry / Langfuse 二选一）
- [x] 增加可视化面板（至少延迟、命中率、错误率）

### 3) 评测回归体系
- [x] 建立最小评测脚本 `eval/run_eval.py`
- [x] 建立最小评测集（4 条）
- [x] 扩展评测集到 30~100 条
- [x] 输出回归对比报告（变更前后）

### 4) 安全基线
- [x] 安全键支持 bcrypt（兼容 SHA256）
- [x] 敏感查询限流（每分钟阈值）
- [x] 基础日志脱敏
- [x] 输入防注入规则（系统化）
- [x] 工具权限白名单策略
- [x] 审计策略与审计日志字段规范

---

## P1（强烈建议）

### 1) 检索质量链路
- [x] Query Rewrite（轻量）
- [x] 检索意图过滤（prefer image/text）
- [x] 轻量 Reranker（规则型）
- [x] 检索流水线模块化（`infra/retrieval/search_pipeline.py`）
- [x] 接入模型级 Reranker

### 2) 索引一致性与恢复
- [x] 文件锁（并发更新保护）
- [x] JSON 原子写入
- [x] 失败回滚机制（索引写入失败可恢复）
- [x] 崩溃恢复标记与恢复流程

### 3) 测试覆盖
- [x] 单元测试基线（当前 11 个用例）
- [x] 一键质量门禁（`scripts/quality_gate.py`）
- [x] 集成测试（检索/降级/update/no-OCR）
- [x] 端到端回归矩阵

---

## P2（加分项）

- [ ] Prompt/模型/策略版本化
- [ ] 灰度发布与回滚
- [ ] 深度接入 Langfuse/OpenTelemetry
- [ ] Web 管理台（指标、开关、诊断）

---

## 每次迭代操作规范（请保持）

1. 开始前：在本清单中勾选本轮目标。
2. 开发后：执行质量门禁：

```bash
python scripts/quality_gate.py
```

3. 验证通过后：更新本清单完成状态（`[ ] -> [x]`）。
4. 同步 README 的“近期改造记录”。
